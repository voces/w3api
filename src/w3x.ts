// import fs from "fs";

// import { luaToJavaScript } from "./lua/luaToJavaScript";

// export const runMap = (war3mapLuaPath: string, entryPath: string): void => {
// 	const war3map = luaToJavaScript(fs.readFileSync(war3mapLuaPath, "utf-8"));

// 	fs.readFileSync(entryPath, "");
// };

// export default runMap;
